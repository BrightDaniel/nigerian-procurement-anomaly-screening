# Data: Processed

This directory is intended for cleaned and processed dataset files.

Currently empty. The application stores processed features directly in the
PostgreSQL database (via the `features` table), not as files.
