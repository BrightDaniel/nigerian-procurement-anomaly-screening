# Data: Synthetic

This directory is intended for synthetic anomaly injection data used in evaluation.

Currently empty. Synthetic anomalies are generated in-memory during evaluation
via `apps/detection/synthetic.py` and are not persisted to disk.
